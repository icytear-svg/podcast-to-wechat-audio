# Hermes 说明 / Hermes Instructions

遵循 `AGENTS.md` 中的仓库级说明。

Follow `AGENTS.md` for repository-wide instructions.

对本项目而言，发布到微信视频号是一个真实公开动作。除非用户明确要求发布，否则使用 `--submit-mode pause`。

For this project, treat public publishing to WeChat Channels as a live action. Use `--submit-mode pause` unless the user explicitly asks to publish.

修改 Playwright 选择器或上传行为前，请先阅读：

Before changing Playwright selectors or upload behavior, read:

- `podcast-to-wechat-audio/references/wechat-channels-audio.md`
- `podcast-to-wechat-audio/references/privacy-and-open-source.md`
